<%# syntax to create form using simple form gem %>

<%= simple_form_for([@post, @post.comments.build]) do |form| %>
    <%= form.input :body, label: "Reply" %>
    <%= form.button :submit, :class => 'btn-styled' %>
<% end %>